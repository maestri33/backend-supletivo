"""Seed do catálogo-base de notificações (`templates.md`) — parser/serializer em `io`.

Não é um app Django; é só um pacote de dados + I/O pro dump/seed. O `templates.md` vive aqui e é
a fonte editável que o `notify_seed_templates` carrega pro DB.
"""