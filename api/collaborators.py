"""Grupo `collaborators` (PLACEHOLDER) — público do funil do PROMOTOR:
candidate → training → promoter. Hoje só o esqueleto; rotas de negócio entram com cada role (§4).
"""

from api.base import build_group

api = build_group("collaborators", "Funil do promotor: candidate, training, promoter.")
